{
  "name": "skills",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "validate": "bun run index.ts validate",
    "sync": "bun run index.ts sync",
    "manifest": "bun run index.ts manifest"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "yaml": "^2.8.2"
  }
}